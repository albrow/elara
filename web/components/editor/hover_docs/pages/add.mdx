#### add

Adds two numbers together and outputs the result.

(This is for teaching purposes only. Normally, you would just use the `+` operator instead.)

##### Examples:

`add(2, 3)` adds the numbers `2` and `3` and outputs the number `5`.
