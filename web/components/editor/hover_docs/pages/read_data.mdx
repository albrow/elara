#### read_data

Outputs the data from a nearby data terminal. Note that this function
does not expect any inputs.

##### Examples:

`let data = read_data();` will read the data from the data terminal and
store it in the variable named `data`.
