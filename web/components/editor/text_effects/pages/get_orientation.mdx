#### get_orientation

Outputs a string depending on which way G.R.O.V.E.R. is currently
facing ("right", "left", "up", or "down").

##### Examples:

`say(get_orientation());` will cause the rover to say which way it is
facing.
