#### read_data

Outputs the data from a nearby data point.

##### Examples:

`say(read_data());` will say whatever the data point holds.

`let data = read_data();` will read the data from the data point and
store it in the variable named `data`.
