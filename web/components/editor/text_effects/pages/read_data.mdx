#### read_data

Outputs the data from a nearby data terminal.

##### Examples:

`say(read_data());` will say whatever the data terminal holds.

`let data = read_data();` will read the data from the terminal and
store it in the variable named `data`.
