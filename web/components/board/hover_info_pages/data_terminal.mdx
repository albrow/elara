##### Data Terminal

Can hold all kinds of information. To interact with one,
move the rover next to it and call the `read_data` function. The
`read_data` function outputs whatever the data terminal holds.
