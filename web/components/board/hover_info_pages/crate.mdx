import { compiler } from "markdown-to-jsx";

##### Crate

Standard issue Ganymede Robotics storage crate.

You can call the `pick_up` function to have G.R.O.V.E.R. pick up the
crate that is directly in front of him. Then, you can use the `drop`
function to drop it.
