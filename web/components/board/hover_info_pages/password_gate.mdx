import { compiler } from "markdown-to-jsx";

##### Password Gate

This gate can be unlocked by moving G.R.O.V.E.R. next to it and
using the `say` function to say the password.

{
props.additionalInfo && (
compiler("##### Additional Info\n\n" + props.additionalInfo)
)
}
