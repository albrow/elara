import { Button } from "@chakra-ui/react";
import { MdArrowForward } from "react-icons/md";
import { Link } from "react-router5";

import { useScenes } from "../hooks/scenes_hooks";

## Welcome to Elara

Elara is a game that teaches you how to code. It is named after one of the
[moons of Jupiter](<https://en.wikipedia.org/wiki/Elara_(moon)>), where the bulk
of the game will take place.

This is a very early build of the game! Things like story, artwork, music and
sound effects, and many gameplay features are not ready yet. There will be
significant changes before the final version of the game is ready to release,
and your feedback can help us get there 😀

Right now, we are focusing on testing the core mechanics of the game, which
includes writing code to control a rover and solve various puzzles. Click the
"Start" button to start playing!

export const StartButton = () => {
  return (
    <Link routeName={"hub"}>
      <Button colorScheme="green">
        <span>Start</span>
        <MdArrowForward size="1.3em" style={{ marginLeft: "0.2rem" }} />
      </Button>
    </Link>
  );
};

<StartButton />

---

### Technical Info

Elara uses a full-featured scripting language called
[Rhai](https://rhai.rs/book/ref/index.html), chosen because it is easy to learn,
runs efficiently, supports WebAssembly, and offers important safeguards such as
limiting the kind of code a user can write and preventing infinite loops. Almost
all of the features of the Rhai language are supported so feel free to read the
Rhai docs and play around a bit.

The source code for Elara is [available on GitHub](https://github.com/albrow/elara).
If you already know a little bit about programming and find a bug somewhere, feel free
to open an issue.
