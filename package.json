{
  "author": "Alex Browne <stephenalexbrowne@gmail.com>",
  "name": "battle-game",
  "version": "0.1.0",
  "license": "MIT or Apache-2.0",
  "scripts": {
    "build": "rimraf dist pkg && node build.js",
    "watch": "rimraf dist pkg && node build.js --watch",
    "test": "cargo test && wasm-pack test --chrome --headless -- --features=wasm"
  },
  "devDependencies": {
    "battle-game-lib": "file:./pkg",
    "esbuild": "^0.15.8",
    "esbuild-plugin-wasm": "^1.0.0",
    "esbuild-plugin-wasm-pack": "^1.1.0",
    "rimraf": "^3.0.0",
    "tailwindcss": "^3.1.8",
    "typescript": "^4.8.2"
  },
  "dependencies": {
    "codemirror": "^6.0.1",
    "pixi.js": "^5.3.12"
  }
}
