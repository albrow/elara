{
  "author": "Alex Browne <stephenalexbrowne@gmail.com>",
  "name": "battle-game",
  "version": "0.1.0",
  "license": "MIT or Apache-2.0",
  "scripts": {
    "build": "rimraf dist pkg && wasm-pack build --dev --target=web -- --features=wasm && node build.js",
    "test": "cargo test && wasm-pack test --chrome --headless -- --features=wasm"
  },
  "devDependencies": {
    "battle-game-lib": "file:./pkg",
    "esbuild-plugin-wasm": "^1.0.0",
    "rimraf": "^3.0.0",
    "typescript": "^4.8.2",
    "esbuild": "^0.15.8"
  },
  "dependencies": {
    "pixi.js": "^5.3.12"
  }
}
